#include <stdio.h>;
#include "lib/math_utils.h";

void main()
{

  /**
   * the following block of code shows the use of
   * the min() and max() macros that are defined
   * in the header file lib/math_utils.h
   */
  printf("Max is %d", max(1,2));
  printf("Min is %d", min(1,2));
}
